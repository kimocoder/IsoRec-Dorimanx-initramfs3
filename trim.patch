diff --git a/res/crontab/cron-scripts/trim-partitions.sh b/res/crontab/cron-scripts/trim-partitions.sh
new file mode 100644
index 0000000..0a4f0bf
--- /dev/null
+++ b/res/crontab/cron-scripts/trim-partitions.sh
@@ -0,0 +1,34 @@
+#!/sbin/busybox sh
+# Trim Partition script
+
+(
+	PROFILE=`cat /data/.siyah/.active.profile`;
+	. /data/.siyah/${PROFILE}.profile;
+
+	if [ "$cron_trim_partitions" == "on" ]; then
+
+		#while [ ! `cat /proc/loadavg | cut -c1-4` \< "3.50" ]; do
+		#	echo "Waiting For CPU to cool down" >> /data/crontab/cron_trim_partitions;
+		#	sleep 30;
+		#done;
+
+		date +%H:%M-%D-%Z >> /data/crontab/cron_trim_partitions;
+		echo "Trim CACHE Partition" >> /data/crontab/cron_trim_partitions;
+                /sbin/fstrim -v /cache  >> /data/crontab/cron_trim_partitions;	
+                
+                echo "Trim System Partition" >> /data/crontab/cron_trim_partitions;
+                /sbin/fstrim -v /system  >> /data/crontab/cron_trim_partitions;
+                
+                echo "Trim DATA Partition" >> /data/crontab/cron_trim_partitions;
+                /sbin/fstrim -v /data  >> /data/crontab/cron_trim_partitions;
+                
+                echo "Trim Preload Partition" >> /data/crontab/cron_trim_partitions;
+                /sbin/fstrim -v /preload  >> /data/crontab/cron_trim_partitions;
+                
+                echo "All-Done" >> /data/crontab/cron_trim_partitions;
+                
+		date +%H:%M-%D-%Z >> /data/crontab/cron_trim_partitions;
+		echo "Done! Trimming Partitions" >> /data/crontab/cron_trim_partitions;
+		sync;
+	fi;
+)&
diff --git a/res/customconfig/actions/crontab b/res/customconfig/actions/crontab
index 44d5812..b57ef2e 100755
--- a/res/customconfig/actions/crontab
+++ b/res/customconfig/actions/crontab
@@ -18,9 +18,11 @@ if [ "a$2" != "a" ]; then
 
 				# setting my jobs with corrected timezone for all world.
 				/res/crontab_service/dm_job.sh "3:00" "/sbin/busybox sh /data/crontab/cron-scripts/database_optimizing.sh"
+				/res/crontab_service/dm_job.sh "3:30" "/sbin/busybox sh /data/crontab/cron-scripts/trim-partitions.sh"
 				/res/crontab_service/dm_job.sh "4:00" "/sbin/busybox sh /data/crontab/cron-scripts/clear-file-cache.sh"
 				/res/crontab_service/dm_job.sh "4:40" "/sbin/busybox sh /data/crontab/cron-scripts/fix-perm.sh"
 				/res/crontab_service/dm_job.sh "4:50" "/sbin/busybox sh /data/crontab/cron-scripts/zipalign.sh"
+				
 			fi;
 		;;
 		off)
diff --git a/res/customconfig/actions/push-actions/crontab_log b/res/customconfig/actions/push-actions/crontab_log
index 9bc2fbc..d304f7c 100755
--- a/res/customconfig/actions/push-actions/crontab_log
+++ b/res/customconfig/actions/push-actions/crontab_log
@@ -1,22 +1,25 @@
 #!/sbin/busybox sh
 
 if [ ! -e /data/crontab/cron-clear-file-cache ]; then
-	echo "no data" > /data/crontab/cron-clear-file-cache;
+	echo "no file cache data" > /data/crontab/cron-clear-file-cache;
 fi;
 if [ ! -e /data/crontab/cron-clear-ram-cache ]; then
-	echo "no data" > /data/crontab/cron-clear-ram-cache;
+	echo "no clear ram data" > /data/crontab/cron-clear-ram-cache;
 fi;
 if [ ! -e /data/crontab/cron-fix_permissions ]; then
-	echo "no data" > /data/crontab/cron-fix_permissions;
+	echo "no fix perm data" > /data/crontab/cron-fix_permissions;
 fi;
 if [ ! -e /data/crontab/cron-zipalign ]; then
-	echo "no data" > /data/crontab/cron-zipalign;
+	echo "no zipalign data" > /data/crontab/cron-zipalign;
 fi;
 if [ ! -e /data/crontab/cron-ad_block_update ]; then
-	echo "no data" > /data/crontab/cron-ad_block_update;
+	echo "no adblock data" > /data/crontab/cron-ad_block_update;
 fi;
 if [ ! -e /data/crontab/cron-db-optimizing ]; then
-	echo "no data" > /data/crontab/cron-db-optimizing;
+	echo "no db opt data" > /data/crontab/cron-db-optimizing;
+fi;
+if [ ! -e /data/crontab/cron_trim_partitions ]; then
+	echo "no trim data" > /data/crontab/cron_trim_partitions;
 fi;
 chmod 777 /data/crontab/*
 cat /data/crontab/cron-clear-file-cache > /data/crontab/cron_log;
@@ -29,8 +32,10 @@ cat /data/crontab/cron-fix_permissions >> /data/crontab/cron_log;
 echo "-------" >> /data/crontab/cron_log;
 cat /data/crontab/cron-db-optimizing >> /data/crontab/cron_log;
 echo "-------" >> /data/crontab/cron_log;
+cat /data/crontab/cron_trim_partitions >> /data/crontab/cron_log;
+echo "-------" >> /data/crontab/cron_log;
 cat /data/crontab/cron-zipalign >> /data/crontab/cron_log;
 
 # push all to one log.
 CRON_LOG=`cat /data/crontab/cron_log`;
-echo $CRON_LOG;
+echo $CRON_LOG;
\ No newline at end of file
diff --git a/res/customconfig/actions/push-actions/trim_partitions b/res/customconfig/actions/push-actions/trim_partitions
new file mode 100644
index 0000000..9e5f7e5
--- /dev/null
+++ b/res/customconfig/actions/push-actions/trim_partitions
@@ -0,0 +1,27 @@
+#!/sbin/busybox sh
+
+/sbin/busybox mount -o remount,rw /;
+echo "START" > /tmp/trim_part;
+chmod 777 /tmp/trim_part;
+
+(
+	echo "Trim CACHE Partition" >> /tmp/trim_part;
+	/sbin/fstrim -v /cache  >> /tmp/trim_part;	
+	
+	echo "Trim System Partition" >> /tmp/trim_part;
+	/sbin/fstrim -v /system  >> /tmp/trim_part;
+	
+	echo "Trim DATA Partition" >> /tmp/trim_part;
+	/sbin/fstrim -v /data  >> /tmp/trim_part;
+	
+	echo "Trim Preload Partition" >> /tmp/trim_part;
+	/sbin/fstrim -v /preload  >> /tmp/trim_part;
+	
+	echo "All-Done" >> /tmp/trim_part;
+)&
+
+while [ ! `cat /tmp/trim_part | grep "All-Done" | wc -l` == "1" ]; do
+	sleep 2;
+done;
+TRIM_LOG=`cat /tmp/trim_part`;
+echo "$TRIM_LOG";
\ No newline at end of file
diff --git a/res/customconfig/customconfig.xml b/res/customconfig/customconfig.xml
index a2db300..ba4199a 100755
--- a/res/customconfig/customconfig.xml
+++ b/res/customconfig/customconfig.xml
@@ -715,6 +737,8 @@
 		<checkbox description="Cron to DB Optimize every day at 3:00AM, only if CPU is resting (IDLE)!" name="DB Optimization" action="generic_cortex /tmp/cron_db_optimizing" label="DB Optimization task every day at 3:00AM"/>
 
 		<checkbox description="Cron to clear app-cache every day at 4:00AM, only if CPU is resting (IDLE)!" name="Clean app-cache" action="generic_cortex /tmp/cron_clear_app_cache" label="Disk Clean task every day at 4:00AM"/>
+                
+                 <checkbox description="Cron to TRIM partitions every day at 3:30AM, only if CPU is resting (IDLE)!" name="TRIM PARTITIONS" action="generic_cortex /tmp/cron_trim_partitions" label="TRIM Partitions system every day at 3:30AM"/>
 
 		<checkbox description="Cron to fix app-permissions every day at 4:40AM, only if CPU is resting (IDLE)! This can help to reduce errors, bugs, and applications FC." name="Fix app-permissions" action="generic_cortex /tmp/cron_fix_permissions" label="Permissions fix every day at 4:40AM"/>
 
@@ -731,6 +755,8 @@
 		<button description="Click here to start Zipaligning ONLY user apps. System may be unresponsive, it is OK, do not use phone for 2 minutes. WARNING! Backup all user applications before use! This process will remove applications that are BROKEN and not compressed or compiled correctly, so you may find some apps missing. USE it AT YOUR OWN RISK, and update the broken applications!." name="ZIPALIGN" action="push-actions/zipalign" label="ZIPALIGN ALL APPS"/>
 
 		<button description="Click here to start applications Database Optimization. Compacting, cleaning and indexing will take max 2min." name="DB OPTIMIZATION" action="push-actions/database_optimizing" label="DATABASE OPTIMIZATON"/>
+                
+                <button description="Click here to start Trimming Partitions. Trimming will take max 2min." name="TRIM-PARTITIONS" action="push-actions/trim_partitions" label="TRIM PARTITIONS"/>
 
 	</settingsPane>
 

